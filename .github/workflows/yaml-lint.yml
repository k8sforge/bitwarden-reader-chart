name: YAML Lint

on:
  pull_request:
    paths:
      - "**/*.yaml"
      - "**/*.yml"
  workflow_call:

permissions:
  contents: read
  checks: write

jobs:
  markdown-lint:
    uses: actionsforge/actions/.github/workflows/yaml-lint.yml@main
